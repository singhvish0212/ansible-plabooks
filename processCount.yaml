---
- hosts: all
  tasks: 
      - name: to check the no of running tasks
        shell: >
                  ps -aux | head -5
        register: result

      - name: show result
        debug:
          var: result
...
